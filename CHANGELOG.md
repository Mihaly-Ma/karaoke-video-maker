# 更新日志

本文件记录每个已发布版本中用户可见的变化。内部重构、格式化与 CI 调整仅在影响
装机或使用时列入。

版本号遵循语义化版本。真源是 `pyproject.toml`，其余落点由 `scripts/version.py --set`
一并改写，tag 与仓库版本由 CI 断言相等。

安装包（macOS `.dmg` / Windows `.exe` / Windows GPU 免安装 zip）见各版本的
[Releases](https://github.com/Mihaly-Ma/karaoke-video-maker/releases) 页面。
安装包未签名与公证，首次打开需绕过 Gatekeeper 或 SmartScreen，方法见 README。

---

## [0.2.3] — 2026-08-17

### 新增

- 编辑舞台版面重排：播放控件图标化；音量与各音轨增益合并为浮窗，音乐 / 人声 /
  引导声改为三个独立开关；声部分配支持在歌词上划词指派；注音编辑移至歌词下方。

### 修复

- 非 16:9 素材上的字号与版面比例。三处成因：ASS 声明的 `PlayRes` 与实际帧尺寸
  不一致、字号仅按高度锚定、非方形像素（SAR ≠ 1）的源在播放时被二次拉伸。
  字号锚点改为按高度并受单行容量封顶，16:9 素材的成片输出逐像素不变。
- 补边到 16:9 的开关现同时作用于预览。此前仅随导出请求发送，而补边改变画布尺寸，
  字号、边距与上下行错开均随之变化。
- 窗口比例变化时字幕被拉伸。字幕画布的 CSS 盒子改用容器查询单位锁定比例。
- 反复切换工程时内存持续累积，最终触发 Out of memory。两处成因：解码结果所在的
  AudioContext 未被可靠关闭；为保音高变速转移至音频线程的整曲样本在 WebKit 上
  不随 AudioContext 关闭而回收，现于释放时转移回主线程。
- 播放时画面与进度条停滞而音频继续。时钟改由音频引擎驱动，视频作为从动方并对
  停滞做纠偏。
- 单行内多个声部未分色显示。各声部段现有独立的连续下划线与声部名。
- 划词高亮的接缝与留白、读音来源标注位置、切换工程时的白屏、波形高度偶发不占满。

**完整提交**：[v0.2.2...v0.2.3](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.2...v0.2.3)

## [0.2.2] — 2026-08-11

### 修复

- Windows GPU 免安装包按体积分卷。GitHub Release 对单个文件有 2 GiB 上限，
  该产物此前在上传阶段失败。

**完整提交**：[v0.2.1...v0.2.2](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.1...v0.2.2)

## [0.2.1] — 2026-08-11

### 修复

- Windows GPU 版改为免安装 zip。NSIS 安装包有 2 GB 硬上限，CUDA 版 torch 必然
  超出。体积闸门移至出壳之前，超限在打包阶段即暴露。

**完整提交**：[v0.2.0...v0.2.1](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.0...v0.2.1)

## [0.2.0] — 2026-08-11

### 新增

- 检测到 NVIDIA 显卡时默认安装 CUDA 版 torch，且不被后续依赖同步覆盖。
  PyPI 的 Windows torch wheel 为 CPU-only。
- Windows GPU 版安装包，产物体积加硬闸门。
- 粗体改用本机粗体字面，不再依赖 libass 合成。

### 修复

- Windows 安装包此前无法实际使用：子进程输出改为显式按 UTF-8 解码，不再随系统
  locale 变化；滤镜图中的路径用单引号包裹；worker 子进程的 PATH 前置私有 ffmpeg
  目录；私有 ffmpeg 路径显式传给 yt-dlp。
- macOS 上 ffmpeg 位置须传文件路径而非所在目录。
- 整曲偏移的下界被制作名单行限制。
- 冷字体请求阻塞约 9 秒。

**完整提交**：[v0.1.0...v0.2.0](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.1.0...v0.2.0)

## [0.1.0] — 2026-08-10

首个公开发布。MIT 许可证，提供 macOS 与 Windows 安装包。

输入 YouTube 链接，输出带逐字扫色歌词与振り仮名的卡拉OK 视频。下载、去人声、
对轴、注音、样式、预览与合成均在本工具内完成；全部推理在本地运行，联网仅限于获取数据。

### 功能

- **素材**：yt-dlp 下载或选择本地文件；人声分离三档；自动生成编辑用代理视频
- **歌词**：QQ 音乐 QRC 逐字轴（含 `[kana:]` 注音轨），或粘贴文本
- **编辑**：波形与逐字时间轴、三级调轴、注音编辑、声部指派、撤销/重做与工程持久化
- **引导声**：从人声提取旋律并合成引导音，可在素材页试听与调参
- **样式**：12 套内置配色，有序字体候选链与字形覆盖预检
- **预览与导出**：预览与成片走同一套 libass 渲染，导出可选 ON / OFF VOCAL 音轨
- **环境**：启动自检与缺失依赖自动获取（带来源与校验和），一律装入应用私有目录
