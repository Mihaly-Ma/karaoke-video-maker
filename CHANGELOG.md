# 更新日志

本文件记录每个已发布版本对**用户可见**的变化。内部重构、纯格式化、CI 调整只在
影响到装机或使用体验时才出现在这里。

版本号遵循语义化版本；版本号的真源是 `pyproject.toml`，其余十余处落点由
`scripts/version.py --set` 一并改写，tag 与仓库版本由 CI 断言必须相等。

安装包（macOS `.dmg` / Windows `.exe` / Windows GPU 免安装 zip）在每个版本的
[Releases](https://github.com/Mihaly-Ma/karaoke-video-maker/releases) 页面。
安装包没有签名与公证，首次打开需绕过 Gatekeeper 或 SmartScreen，做法见 README。

---

## [0.2.3] — 2026-08-17

这一版集中修画幅与编辑器：字幕在非 16:9 素材上的比例、以及长时间使用后的内存。

### 新增

- **编辑舞台版面重排。** 播放控件改为图标，音量与各音轨增益收进一个浮窗（音乐 /
  人声 / 引导声三个独立开关，不再是互斥档位）；声部分配改为在歌词上划词指派；
  注音编辑从远处的侧栏挪到歌词正下方，输入框数量大幅收敛。

### 修复

- **非 16:9 画面上字的比例不对。** 有三个互不相同的来源却是同一种症状：ASS 声明的
  `PlayRes` 与实际帧尺寸不符、字号只锚定高度、以及非方形像素（SAR ≠ 1）的源在播放器里
  被再拉伸一次。字号锚点改为"以高度为准、受一行放得下封顶"，16:9 素材的成片逐像素不变。
- **补边到 16:9 的开关现在同时作用于预览。** 此前它只随导出请求发出，勾了在预览里
  看不出任何变化——而补边改变画布尺寸，字号、边距、上下行错开、一行放几个字全都跟着变，
  用户只能靠导出成片来确认版面。
- **改变窗口比例时字幕跟着变形。** 字幕画布的 CSS 盒子此前会被布局压扁，而同一个盒子里的
  视频有 `object-fit` 顶着，于是只有字幕变形。改用容器查询单位把盒子比例锁死。
- **来回切换工程时内存持续累积，最终报 Out of memory。** 两条独立原因：解码结果所在的
  AudioContext 未被可靠关闭；以及为保音高变速而转移进音频线程的整曲样本，在 WebKit 上
  不随 AudioContext 关闭而回收——现在释放时把样本转移回主线程再关闭。
- **播放中画面与进度条会停住而声音继续。** 时钟改由音频引擎驱动、视频作为从动方，
  并对停滞的视频做纠偏。
- **一行内多个声部现在真的分色显示**，每段有自己的连续下划线与声部名。
- 划词高亮的接缝、留白与来源标注位置；切换工程时的白屏；波形高度偶发不占满。

**完整提交**：[v0.2.2...v0.2.3](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.2...v0.2.3)

## [0.2.2] — 2026-08-11

### 修复

- **Windows GPU 免安装包按体积分卷。** GitHub Release 对单个文件有 2 GiB 上限，
  CUDA 版 torch 使该产物越过了这条线，此前会在上传阶段失败。

**完整提交**：[v0.2.1...v0.2.2](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.1...v0.2.2)

## [0.2.1] — 2026-08-11

### 修复

- **Windows GPU 版改为免安装 zip。** NSIS 安装包有 2 GB 硬上限，装进 CUDA 版 torch
  必然撞穿；同时把体积闸门挪到出壳之前，让超限在打包早期就暴露而不是在上传时。

**完整提交**：[v0.2.0...v0.2.1](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.2.0...v0.2.1)

## [0.2.0] — 2026-08-11

首个可用于 Windows 的版本 —— v0.1.0 出包后在真机上暴露的问题在这一版集中修完。

### 新增

- **检测到 NVIDIA 显卡时默认安装 CUDA 版 torch**，且不会被后续的依赖同步冲掉。
  PyPI 的 Windows torch wheel 是 CPU-only 的，此前用户会在毫不知情的情况下用 CPU 跑分离。
- **提供 Windows GPU 版安装包**，并给产物体积加了硬闸门。
- **粗体改用本机的真粗体字面**，不再依赖 libass 合成粗体。

### 修复

- **Windows 上的子进程编码问题在全库一次修完**：输出显式按 UTF-8 解码，不再随系统
  locale 变化；滤镜图里的路径用单引号包裹；worker 子进程的 PATH 前置私有 ffmpeg 目录；
  并把私有目录的 ffmpeg 显式告诉 yt-dlp。
- **macOS 上 ffmpeg 位置传的是文件路径而非所在目录。**
- 整曲偏移的下界不再被制作名单行卡住；冷字体请求不再阻塞约 9 秒。

**完整提交**：[v0.1.0...v0.2.0](https://github.com/Mihaly-Ma/karaoke-video-maker/compare/v0.1.0...v0.2.0)

## [0.1.0] — 2026-08-10

首个公开发布，MIT 许可证，提供 macOS 与 Windows 安装包。

一条 YouTube 链接进、一个带逐字扫色歌词与振り仮名的卡拉OK 视频出，全过程在本工具内
完成，不需要打开 Aegisub 或任何第二个软件。所有推理（人声分离、音高提取）都跑在本地，
联网仅限于取数据。

主要能力：

- **素材**：yt-dlp 下载或选本地文件；人声分离三档；自动生成编辑用代理视频
- **歌词**：QQ 音乐 QRC 逐字轴（含 `[kana:]` 注音轨），或粘贴文本
- **编辑**：波形 + 逐字时间轴，三级调轴，注音编辑，声部指派，撤销/重做与工程持久化
- **引导声**：从人声提取旋律并合成电子引导音，可在素材页试听调参
- **样式**：12 套内置配色，有序字体候选链与字形覆盖预检
- **预览与导出**：预览与成片走同一套 libass 渲染，导出可选 ON / OFF VOCAL 音轨
- **环境**：启动自检 + 缺失依赖自动获取（知情下载，带来源与校验和），一律装进应用私有目录
